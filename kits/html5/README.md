# Pure HTML5 Starter Kit

This starter kit uses pure HTML5 and requires no build system.

## Features

- Loads the `pocketbase` JS SDK
- Connects to a PocketBase endpoint
- Calls `/api/health` and displays the result
